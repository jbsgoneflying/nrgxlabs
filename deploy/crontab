# NRGX scheduled jobs (all times UTC)
# Logs go to /var/log/cron.log inside the container
#
# Ichimoku-only desk (2026-08-07): every other engine is offline in full,
# so their refresh/scan jobs were removed. The Ichimoku scanner (Engine 4)
# runs on demand from the /ichimoku page and needs no scheduled jobs —
# EODHD bars are fetched per scan and cached in-process.
